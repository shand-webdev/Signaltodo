import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Signal | Find the Signal in Your Chaos",
  description: "An AI-powered daily clarity tool. Dump everything on your mind in the morning and instantly focus on the signal, filtering out the noise. Inspired by Apple and Arc design paradigms.",
  authors: [{ name: "Signal Team" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
      style={{ colorScheme: "dark" }}
    >
      <body className="min-h-full flex flex-col text-slate-100 bg-[#080C18] selection:bg-amber-500/20 selection:text-amber-500">
        {children}
      </body>
    </html>
  );
}
